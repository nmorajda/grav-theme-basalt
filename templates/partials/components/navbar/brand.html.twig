{#
    Navbar brand component.

    Renders the site title linked to the homepage.
    Override this template to provide a custom brand or logo.
#}

<a class="navbar-brand" href="{{ home_url|e('html_attr') }}">
    {{ site.title|e }}
</a>