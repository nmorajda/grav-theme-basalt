{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="error-page">
        <h1>{{ page.title|e }}</h1>

        <div class="error-page__content">
            {{ page.content|raw }}
        </div>
    </div>
{% endblock %}